Sub ConditionnalFormatting()

For a=2 to Rows.Count

Yearly_Change = Cells(a,11).Value
    
    If Yearly_Change > 0 Then
    Yearly_Change.Interior.Color = RGB(255, 0, 0)

    ElseIf Yearly_Change < 0 Then
    Yearly_Change.Interior.Color = RGB(0, 255, 0)

    End If

  Next a

End Sub
